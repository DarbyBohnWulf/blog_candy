<h1 id="title">Articles tagged <%= @tag.name %></h1>
<ul id="articles" class="list-group">
  <% @tag.articles.each do |article| %>
    <li class="list-group-item">
      <%= link_to article.title, article_path(article), class: 'article_title' %>
    </li>
  <% end %>
</ul>
