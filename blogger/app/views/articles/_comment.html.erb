<div class="comment">
  <h4 class="comment_author">Comment by <%= comment.author %></h4>
  <p class="comment"><%= comment.body %></p>
  <small class="comment-timestamp">posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</small>
</div>
